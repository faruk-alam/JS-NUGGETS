// formula: we all know that-
// Area of triangle = (base value * heigt value) / 2.

const baseValue = Number(prompt("Enter base value of triangle"));
const heigtValue =Number(prompt("Enter height value of triangle"));
const areaOftriangle = (baseValue * heigtValue) / 2;
document.write("The Area of triangle is  :" + areaOftriangle);